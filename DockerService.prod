FROM golang:1.25-alpine AS builder
ARG SERVICE_NAME
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o service ./backend/services/${SERVICE_NAME}/app/main.go

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/service ./service
EXPOSE 8080
CMD ["./service"]
