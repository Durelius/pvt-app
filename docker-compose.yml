services:

  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    ports:
      - "8080:8080"
    volumes:
      - ./services:/services

  auth:
    build: ./services/auth/
    container_name: auth

